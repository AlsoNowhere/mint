const mint = require("../../dist/mint-test");

const { app, element } = mint;

describe("When moving the position of an Object in a mFor list", () => {
  test("Should update the index but preserve the Object reference", () => {});

  test("Should keep the object reference in sync with the original Object", () => {});
});

describe("When substituting an Object for a similar but different Object", () => {
  test("Should keep the same index", () => {});

  test("Should update the '_x' reference to this new Object.", () => {});
});
