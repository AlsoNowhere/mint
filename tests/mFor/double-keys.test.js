const mint = require("../../dist/mint-test");

const { app, element } = mint;

describe("If two objects, strings or numbers are the same on a list then error", () => {
  test("Should throw an error");
});

describe("If two objects, strings or numbers are the same on a list, AFTER A REFRESH then error", () => {
  test("Should throw an error");
});
