import { Blueprint } from "../../models/blueprint/Blueprint.model";
import { TElement } from "../../types/TElement.type";
export declare const renderBlueprints: (blueprints: Array<Blueprint>, parentElement: TElement, parentChildBlueprints?: Array<Blueprint>, indexes?: Array<number>) => void;
