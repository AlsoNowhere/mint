import { Blueprint } from "../../models/blueprint/Blueprint.model";
import { TElement } from "../../types/TElement.type";
export declare const addList: (list: Array<Blueprint>, childBlueprints: Array<Blueprint>, parentElement: TElement, blueprintIndex: number) => void;
