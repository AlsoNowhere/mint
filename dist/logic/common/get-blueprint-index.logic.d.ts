import { Blueprint } from "../../models/blueprint/Blueprint.model";
export declare const getBlueprintIndex: (blueprint: Blueprint, initialBlueprint?: Blueprint) => {
    blueprintList: Array<Blueprint>;
    blueprintIndex: number;
};
