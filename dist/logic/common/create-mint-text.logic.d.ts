import { INode } from "../../interfaces/INode.interface";
import { TRawContent } from "../../types/TRawContent.type";
export declare const createMintText: (initialContent: null | TRawContent | INode | Array<TRawContent | INode>) => Array<INode>;
