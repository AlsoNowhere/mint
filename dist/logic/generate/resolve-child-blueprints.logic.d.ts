import { Blueprint } from "../../models/blueprint/Blueprint.model";
export declare const resolveChildBlueprints: (blueprint: Blueprint, childBlueprints: Array<Blueprint>, isSVG: boolean) => Array<Blueprint>;
