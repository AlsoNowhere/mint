import { MintElement } from "../models/MintElement.model";

export type TMintElementTemplate = {
  element: MintElement;
  attributes: Object;
  component?: never;
  props?: never;
  text?: never;
};
